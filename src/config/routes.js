const Home = '/';
const Login = `${Home}Login`;
const Register = `${Home}Register`;
const Category = `${Home}categories/:id/:name`;
const BookDetail = `${Home}book/:id`;
const CustomSearch = `${Home}custom`

export default {
    Home,
    Login,
    Register,
    Category,
    BookDetail,
    CustomSearch
}